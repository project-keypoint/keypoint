<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itwillbs.mappers.lineMapper">


	<select id="getLineList" resultType="com.keypoint.dto.LineDTO">
		SELECT line.*, workOrder.woCode
		FROM line
		LEFT JOIN workOrder ON line.lineCode = workOrder.lineCode
		ORDER BY line.lineCode DESC;
	</select>





</mapper>




