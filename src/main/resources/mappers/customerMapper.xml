<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  
<mapper namespace="com.itwillbs.mappers.customerMapper">



	<select id="getCusList" resultType="com.keypoint.dto.CustomerDTO">
		SELECT *
		FROM customer
		ORDER BY cusCode desc
	</select>


	<insert id="insertCustomer">
		INSERT INTO customer (cusName, cusCategory, cusBusiness, cusType, cusNumber, cusRep,
								cusTel, cusHome, cusAddress, cusResp, cusPhone, cusEmail,
								cusFax, cusMemo, cusStatus, cusDate)
		VALUES(
				#{cusName}, #{cusCategory}, #{cusBusiness}, #{cusType}, #{cusNumber}, #{cusRep},
				#{cusTel}, #{cusHome}, #{cusAddress}, #{cusResp}, #{cusPhone}, #{cusEmail},
				#{cusFax}, #{cusMemo}, #{cusStatus}, #{cusDate})
	</insert>



	














</mapper>
